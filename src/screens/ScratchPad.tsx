import React from 'react';
import { Paper } from '@material-ui/core';

export const ScratchPad = () => {
	return (
		<Paper style={{ padding: '10px', minHeight: '80vh' }}>
			A place to store some notes and links with a bit of organization
		</Paper>
	);
};
